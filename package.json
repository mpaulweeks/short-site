{
  "name": "short-site",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "bs": "yarn tsc-utils && yarn tsc-store && lerna bootstrap",
    "build:app": "yarn bs && cd packages/app && yarn build",
    "deploy:app": "yarn build:app && gh-pages -d packages/app/build",
    "dev:files": "http-server local --cors=If-Modified-Since, Range",
    "run:api": "cd packages/api && yarn start",
    "run:app": "cd packages/app && yarn start",
    "run:scrape": "cd packages/store && yarn scrape",
    "setup": "yarn install && yarn bs",
    "tsc-store": "cd packages/store && yarn tsc",
    "tsc-utils": "cd packages/utils && yarn tsc"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "http-server": "^0.11.1",
    "lerna": "^3.13.4",
    "typescript": "^3.4.5"
  }
}
